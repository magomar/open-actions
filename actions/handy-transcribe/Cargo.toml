[package]
name = "handy-transcribe"
version = "0.1.0"
edition = "2024"

[dependencies]
openaction = "2.7.0"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
